## 1. Theme Configuration

- [x] 1.1 Update `src/themes/light-theme.ts` with Navy/Cyan color palette.
- [x] 1.2 Override `MuiButton` to include a subtle gradient and rounded corners.
- [x] 1.3 Update `MuiCard` and `MuiAccordion` styles to have softer shadows and slight transparency.

## 2. Layout & Global Styles

- [x] 2.1 Update `src/app/globals.css` to add a subtle gradient or structured background to the entire page body.
- [x] 2.2 Add a glassmorphic blur to the top navigation (`Menu.tsx` / `MuiAppBar`).

## 3. Verify

- [x] 3.1 Open the browser and verify the `/` inputs and `/viewer` outputs render correctly against the new aesthetic.
